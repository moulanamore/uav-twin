# Eclipse Mosquitto config — dev only.
# Two listeners: raw MQTT for services, WebSocket for the browser dashboard.
# Anonymous access is enabled; harden before exposing to a network.

persistence true
persistence_location /mosquitto/data/
log_dest stdout

listener 1883
protocol mqtt
allow_anonymous true

listener 9001
protocol websockets
allow_anonymous true
